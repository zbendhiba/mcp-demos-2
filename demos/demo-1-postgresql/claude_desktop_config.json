{
    "mcpServers": {
        "postgres": {
        "command": "npx",
        "args": [
          "-y",
          "@modelcontextprotocol/server-postgres",
          "postgresql://myuser:your_secret_password@localhost:5432/mydatabase"
        ]
      }
  }
}